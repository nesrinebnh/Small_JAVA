protected class_SJ Test{
        string_SJ b,m,g,g,l;
        int_SJ d,e,f,c;
        float_SJ elfhghjfhrjrjk,w;
        main_SJ{
            a := 5 + 2 ;
            d := f + m;
            c := "25"*"12";
            e := 12.25;
            f := 1 - 1;

            si( ( a > "salam" ) | ( 31 >= 25 ) & ( 5 > 20 ) | ( 26 != 13 )  )
            alors{
                e := a * d + ( a + d ) / f;
                OUT_SJ('hello %f',a);

            }
            sinon{
                IN_SJ('%s',b);
            }


        }
    }